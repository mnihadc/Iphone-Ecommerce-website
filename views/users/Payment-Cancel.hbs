<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --midnight-blue: #1D1E22;
            --space-gray: #3A3B3F;
            --silver: #F5F5F7;
            --alpine-green: #4A5842;
            --gold: #F8D12F;
            --pacific-blue: #007AFF;
            --graphite: #8E8E93;
            --sunrise-pink: #FFC0CB;
            --charcoal-black: #121212;
            --slate-gray: #636366;
            --white: #FFFFFF;
            --warning-yellow: #FFC107;
        }

        .waiting-card {
            max-width: 600px;
            margin: 50px auto;
            opacity: 0;
            animation: fadeIn 1s forwards;
            background-color: var(--silver);
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
            }

            100% {
                opacity: 1;
            }
        }

        .btn-primary {
            background-color: var(--midnight-blue);
            border-color: var(--midnight-blue);
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background-color: var(--pacific-blue);
            border-color: var(--pacific-blue);
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .icon {
            font-size: 5rem;
            color: var(--warning-yellow);
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.1);
            }

            100% {
                transform: scale(1);
            }
        }

        .order-summary {
            background-color: var(--silver);
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
            box-shadow: 0 3px 5px rgba(0, 0, 0, 0.1);
        }

        .order-summary h5 {
            color: var(--pacific-blue);
        }

        .progress-label {
            text-align: center;
            margin-top: 5px;
            font-weight: bold;
        }

        .waiting-message {
            color: var(--warning-yellow);
            font-size: 1.25rem;
            font-weight: bold;
            margin-top: 15px;
        }

        .order-id {
            font-size: 1.1rem;
            color: var(--pacific-blue);
        }
    </style>
</head>

<body style="background-color: var(--space-gray);">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-6">
                <div class="card shadow-lg waiting-card">
                    <div class="card-body text-center">
                        <!-- Waiting Icon -->
                        <div class="icon mb-4">
                            <i class="bi bi-hourglass-split"></i>
                        </div>

                        <!-- Waiting Title -->
                        <h3 class="card-title" style="color: var(--midnight-blue);">{{title}}</h3>
                        <p class="lead text-muted">{{message}}</p>

                        <!-- Waiting Confirmation Message -->
                        <div class="waiting-message">
                            <p>Your order is waiting for payment ⏳</p>
                            <p class="order-id">Order ID: #{{checkout._id}}</p>
                        </div>

                        <!-- Waiting Progress -->
                        <div class="progress-bar-container">
                            <label for="orderProgress" class="progress-label">Payment Status:</label>
                            <div class="progress">
                                <div class="progress-bar bg-warning" style="width: 50%;" role="progressbar"
                                    aria-valuenow="50" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                        </div>

                        <!-- Back to Orders Button -->
                        <a href="/order/get-orders" class="btn btn-primary mt-4">Go Back to Orders</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS & Icons -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
</body>

</html>