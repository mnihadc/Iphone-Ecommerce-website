<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>

</head>

<body>
    <div class="coupon-page">
        <div class="container">
            <h1>Your Coupons</h1>
            <div class="coupons">
                {{#if coupons.length}}
                {{#each coupons}}
                <div class="coupon">
                    <h2>{{this.name}}</h2>
                    <p><strong>Code:</strong> {{this.code}}</p>
                    <p><strong>Discount:</strong> {{this.discountPercentage}}%</p>
                    <p><strong>CreatedAt:</strong> {{this.createdAt}}</p>
                    <p><strong>Expiry Date:</strong> {{this.validUntil}}</p>
                    <span class="status {{this.status}}">{{this.status}}</span>
                </div>
                {{/each}}
                {{else}}
                <p>No coupons available.</p>
                {{/if}}
            </div>
        </div>
    </div>
</body>

</html>