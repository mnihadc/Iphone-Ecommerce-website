<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>iStore Chatbot</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(to right, #6212b8, #02f6f2, #037d98);
            color: #fff;
            height: 100vh;
            align-items: center;
        }

        .chatbot-head {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            margin-top: 1rem;
        }

        .chatbot-container {
            background-color: #ffffff;
            color: #333;
            width: 100%;
            height: 500px;
            max-width: 700px;
            border-radius: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            position: relative;
            margin-bottom: 5rem;
        }

        .chatbot-header {
            background: linear-gradient(to right, #007bff, #00c6ff);
            padding: 15px;
            text-align: center;
            font-size: 20px;
            font-weight: bold;
            color: #fff;
        }

        .chatbot-body {
            flex: 1;
            padding: 15px;
            display: flex;
            flex-direction: column;
            gap: 10px;
            overflow-y: auto;
            position: relative;
        }

        .message {
            padding: 10px;
            border-radius: 10px;
            max-width: 80%;
            word-wrap: break-word;
        }

        .message.user {
            background-color: #007bff;
            color: #fff;
            align-self: flex-end;
        }

        .message.bot {
            background-color: #f1f1f1;
            color: #333;
            align-self: flex-start;
        }

        .chatbot-footer {
            padding: 10px;
            background-color: #f9f9f9;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .chatbot-footer input {
            flex: 1;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            outline: none;
            font-size: 16px;
        }

        .chatbot-footer button {
            padding: 10px 20px;
            background-color: #007bff;
            border: none;
            color: #fff;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
        }

        .chatbot-footer button:hover {
            background-color: #0056b3;
            transform: scale(1.05);
        }

        .chatbot-image {
            background: linear-gradient(to right, #6212b8, #02f6f2, #037d98);
            text-align: center;
            padding: 20px;
            width: 100%;
            position: relative;
            height: 4rem;
        }

        .circle-image {
            position: absolute;
            bottom: -30px;
            width: 60px;
            height: 60px;
            border: 2px solid #fff;
            border-radius: 50%;
            margin-bottom: 2rem;
        }

        .circle-image.left {
            left: 10px;
            background: url('https://www.shutterstock.com/image-vector/chat-bot-logo-design-concept-600nw-2478937557.jpg') no-repeat center/cover;
        }

        .circle-image.right {
            right: 10px;
            background: url('https://t4.ftcdn.net/jpg/02/29/75/83/360_F_229758328_7x8jwCwjtBMmC6rgFzLFhZoEpLobB6L8.jpg') no-repeat center/cover;
        }

        .scroll-button {
            display: none;
            position: absolute;
            bottom: 70px;
            right: 20px;
            width: 40px;
            height: 40px;
            background: #007bff;
            border: none;
            border-radius: 50%;
            color: #fff;
            font-size: 24px;
            cursor: pointer;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s, background-color 0.3s;
        }

        .scroll-button:hover {
            background-color: #0056b3;
            transform: scale(1.1);
        }

        @media screen and (max-width: 480px) {
            .chatbot-header {
                font-size: 18px;
            }

            .chatbot-footer button {
                font-size: 14px;
            }

            .circle-image {
                width: 50px;
                height: 50px;
            }
        }
    </style>
</head>

<body>
    <div class="chatbot-head">
        <div class="chatbot-container">
            <div class="chatbot-header">Need Help? Chat with iStore Bot!</div>
            <div class="chatbot-image">
                <div class="circle-image left"></div>
                <div class="circle-image right"></div>
            </div>
            <div class="chatbot-body" id="chat">
                <!-- Chat messages will be dynamically inserted here -->
            </div>
            <button class="scroll-button" id="scrollButton" onclick="scrollToBottom()">&#8595;</button>
            <div class="chatbot-footer">
                <input type="text" id="userInput" placeholder="Type your question...">
                <button onclick="sendMessage()">Send</button>
            </div>
        </div>
    </div>
    <script src="/js/ChatBot.js"></script>
</body>

</html>